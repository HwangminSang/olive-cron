<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" />

    <title>Reset password</title>

    <style>
      @import url('https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap');

      html,
      body,
      .wrapper {
        height: 100%;
      }
      html,
      body,
      div,
      header,
      h1,
      h2,
      h3,
      p,
      input,
      button {
        font-family: 'Roboto', sans-serif;
      }
      img {
        transform: translateZ(0);
        image-rendering: -webkit-optimize-contrast;
        backface-visibility: hidden;
      }

      .sr-only {
        position: absolute;
        width: 0px;
        height: 0px;
        padding: 0;
        margin: -1px;
        border: 0;
        clip: rect(0, 0, 0, 0);
        overflow: hidden;
      }

      .wrapper {
      }
      .wrapper header {
        justify-content: center;
        padding: 0;
        height: 56px;
        text-align: center;
      }
      .wrapper header h1 {
        display: flex;
        align-items: center;
        margin-bottom: 0;
      }
      .wrapper header h1 picture img {
        display: flex;
        max-width: 92px;
      }
      .wrapper header h1 span {
        display: inline-flex;
        margin-left: 12px;
        font-size: 20px;
        color: #fff;
      }

      .wrapper main {
        width: 380px;
        max-width: 380px;
        margin: 30px auto;
      }
      .wrapper main .guidance {
        padding: 20px 16px;
        border-radius: 6px;
        border: solid 1px #ea4118;
        background-color: rgba(234, 65, 24, 0.1);
      }
      .wrapper main .guidance h3 {
        margin: 0;
        padding-bottom: 4px;
        font-size: 16px;
        color: #ea4118;
        line-height: 1.75;
      }
      .wrapper main .guidance p {
        margin: 0;
        font-size: 16px;
        color: #1c1c1f;
        line-height: 1.5;
      }

      .wrapper #ResetPwdForm {
        margin-top: 40px;
      }
      .wrapper #ResetPwdForm input {
        display: block;
        height: 56px;
      }
      .wrapper #ResetPwdForm span.error {
        display: block;
        margin-top: 8px;
        font-size: 16px;
        color: #ea4118;
      }
      .wrapper #ResetPwdForm input#confirmedPassword {
        margin-top: 15px;
      }
      .wrapper #ResetPwdForm button {
        height: 56px;
        margin-top: 40px;
        border-radius: 6px;
        background-color: rgba(52, 92, 248, 0.7);
        font-weight: bold;
        font-size: 20px;
        color: #fff;
      }
    </style>
  </head>
  <body>
    <div class="wrapper">
      <header class="navbar bg-black">
        <h1>
          <picture>
            <source media="(min-width:600px)" srcset="../../images/logo@2x.png" />
            <source media="(min-width:1200px)" srcset="../../images/logo@3x.png" />
            <img src="../../images/logo.png" alt="Olive" style="width: auto" />
          </picture>
          <span>Password Reset</span>
        </h1>
      </header>

      <main>
        <h2 class="sr-only">Page for resetting a password</h2>
        <div class="guidance">
          <h3>Please enter a new password of 8 or more digits.</h3>
          <p>It is safe to set the password using a combination of uppercase and lowercase letters and numbers.</p>
        </div>

        <form id="ResetPwdForm" class="needs-validation" action="/api/v1/reset-password/post" method="POST">
          <input type="hidden" name="email" value="<%=datas.email%>" />
          <input type="hidden" name="code" value="<%=datas.code%>" />
          <div>
            <input type="password" class="form-control" id="password" placeholder="New password" name="password" />
            <% if (errors !== undefined){ %> <% for (const error of errors){ %> <% if (error.param === 'password' && error.msg !== 'Invalid value'){ %>
            <span class="error"><%= error.msg %></span>
            <% } %> <% } %> <% } %>
          </div>
          <div>
            <input type="password" class="form-control" id="confirmedPassword" placeholder="Re-enter New Password" name="confirmedPassword" />
            <% if (errors !== undefined){ %> <% for (const error of errors){ %> <% if (error.param === 'confirmedPassword'){ %>
            <span class="error"><%= error.msg %></span>
            <% } %> <% } %> <% } %>
          </div>

          <div class="d-grid">
            <button type="submit" class="btn">Reset password</button>
          </div>
        </form>
      </main>
    </div>
  </body>
</html>
